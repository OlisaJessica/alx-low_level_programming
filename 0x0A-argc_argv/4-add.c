#include <stdio.h>
#include <stdlib.h>
#include  "main.h"
/**
 * main -adds positive numbers
 * @argc: counts the arguments
 * @argv:Arguments
 *
 * Return ; 0 on success
 * Or when in error 1
 */

int main(int argc, char *argv[])
{
int i;
int sum;
sum = argv[0] + argv[0];
if
(i < 0, i++);
{
printf("%d\n", i++);

return (0);
} while
(i > 0, i--);
{
printf("%d\n", i--);
return (1);
printf("%d\n", sum);
}
}
